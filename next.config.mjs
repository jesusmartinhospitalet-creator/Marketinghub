/** @type {import('next').NextConfig} */
const nextConfig = {
  // Compatible with standard Node.js hosting (Hostinger)
  // Use 'standalone' for self-contained deployments
  // output: 'standalone',
}

export default nextConfig
